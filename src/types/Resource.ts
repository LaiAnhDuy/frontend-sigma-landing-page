export interface ResourceTypes {
  resources: string[];
  blogs: string[];
}
export interface BlogTypes {
  id: number;
  title: string;
  author: string;
  markdown: string;
  option: string;
  imageUrl: string;
}
