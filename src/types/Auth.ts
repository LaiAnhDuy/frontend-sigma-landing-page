export interface AuthTypes {
  token: string;
  user: string[];
  logIn: boolean;
  tokenExpired: boolean;
}

export interface User {
  listUser: string[];
}

export interface User {
  listUser: string[];
  modalOpen: boolean;
}
